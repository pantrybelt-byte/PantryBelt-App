# AccessBelt — Project Constitution (claude.md)

**This file is law.** Update only when schemas, rules, or architecture change.

---

## Governance & platform scope

Source of truth: AccessBelt LLC Business Plan and Operating Agreement (not in this repo — company records). Summarized here only where it constrains engineering work.

- **Entity:** AccessBelt LLC, an Alabama LLC, owned 50/50 by Matthew Clarke and Thaddaus Sneed (Co-Managers, equal authority). All pantry data, the "AccessBelt" name/mark, and the in-app assistant character (Pete) are Company IP.
- **Platform scope is legally restricted, not just a product choice (Operating Agreement Art. VII):** during the current "Initial Deployment Phase," the platform serves exactly two constituencies — **Users** (mobile app) and **Agencies** (pantries, via the future operational dashboard). Do not build or wire up features that provision the platform to a third constituency — government data-licensing customers, commercial data buyers, or advertisers — without explicit confirmation that both Members gave unanimous written consent (a "Major Decision"). This was already considered and deliberately rejected once (an anonymized-data-sales model to state agencies) — treat any revival of that direction as requiring the same unanimous sign-off, not an engineering judgment call.
- **Privacy is a governance commitment, not just a default (Operating Agreement §6.5):** never store precise end-user location; retain only county-level geographic granularity for analytics. Any change to data collection practices is itself a Major Decision requiring unanimous consent — do not loosen this to "make analytics better" without that.
- **Revenue model (Business Plan §4.5):** individual Users never pay, are never shown ads, and their individual-level data is never sold. Monetization is Agency-side only (dashboard subscription tiers, not yet built). Don't add consumer payment/ad code against this app without checking that against the plan first.

---

## Data schemas

### App state (in-memory unless noted)

- **Auth:** Hybrid, anonymous-by-default. Firebase Anonymous Auth runs on every launch via `utils/auth.ts`'s `initAppSecurity()` (Tier 1) — no account needed to use the app. A companion `_app_sessions/{uid}` doc (Tier 3A) is bootstrapped alongside it so Firestore rules can confirm writes come from the real app binary. Real email/password accounts are an **optional upgrade**, reachable from Profile → Account → `/(auth)/signin` (`utils/auth.ts`'s `signUpWithEmail()`/`signInWithEmail()`/`signOutUser()`). Signing up links the credential to the existing anonymous uid via `linkWithCredential()` — the uid never changes, so `user_profiles/{uid}` and session data carry over automatically with no migration step. Signing out returns to a fresh anonymous identity (the previous account's data stays intact in Firestore under its own uid, untouched, ready for the next sign-in). Because switching identity changes `auth.currentUser.uid`, every sign-up/sign-in/sign-out calls `reestablishSession()` to force an immediate `_app_sessions/{newUid}` bootstrap — `initAppSecurity()`'s own memoization is time-based, not uid-based, and would otherwise leave the new uid failing `hasActiveSession()` for up to 10 minutes.
- **Profile (`app/(tabs)/profile.tsx`):** A settings screen — dark mode, push notifications, location, newsletter toggles. Push, location, and newsletter persist to AsyncStorage; the newsletter opt-in additionally syncs to `user_profiles/{uid}.newsletter` (best-effort, like `pushToken`). Dark mode is owned by `context/ThemeContext.tsx`.
- **Demographic Profile (Firestore: `user_profiles/{uid}`)** — optional, user-entered, keyed by the anonymous Firebase uid, not linked to any real-world identity:
  - `age: number` (13–120)
  - `familySize: number` (1–20)
  - `zipCode: string` (5-digit)
  - `race: string | null` (optional; one of the standard census-style categories in `utils/userProfile.ts`'s `RACE_OPTIONS`, or `prefer_not_to_say`)
  - `pushToken: string | null` (optional, set when push notifications are enabled)
  - `newsletter: boolean` (optional, Profile → Newsletter toggle opt-in)
  - `updatedAt: serverTimestamp`
  - Read/write via `utils/userProfile.ts` (`getUserProfile()` / `saveUserProfile()`); gated on `useAuthReady()` before reading.
- **Pantries (Firestore: `agencies/{id}`):** Loaded live in `app/(tabs)/map.tsx` via `fetchPantries()`, querying `where('status', '==', 'active')`. Firestore fields: `orgId, name, locationType, status, county, coordinates: {lat, lng}, geohash, address: {street, city, county, state, zip}, hours (string | per-day object | null), phone, website, socialMedia: string[] | null, eligibilityNotes, docsRequired: string[], serviceRadiusMiles, capacity, tags: string[], verified, operatorPortalAccess: boolean, miniProfile: object | null, createdBy, createdAt, updatedAt`. Mapped client-side into the screen's flat `Pantry` type `{ id, name, city, county, lat, lng, phone, address, hours, eligibility, docs, website, verified, socialMedia, operatorPortalAccess, hasMiniProfile }`; docs missing valid non-zero coordinates are filtered out. Map pins/badges use a 3-tier color scheme computed client-side by `pantryTier()` in `map.tsx` — **grey** (default, active but no reported activity), **orange "Active"** (has a `phone`, `website`, or `socialMedia` entry), **green "Verified"** (`verified === true` AND `operatorPortalAccess === true` AND `miniProfile` set). None of this gates map visibility, so it's safe to correct without hiding pantries. `operatorPortalAccess`/`miniProfile` are placeholders for the future Agency Operator Portal (not yet built) — nothing sets them yet, so no pantry shows green today; that's expected. `verified: true` means an individual has confirmed the org still exists via web search or phone (a separate, human-driven signal from the orange/green activity tiers above); it must never be blanket-set across many docs at once (see 2026-09-01 maintenance entry) — see `tools/applyVerificationResults.js` and `tools/.tmp/verify/` for the batch web-verification workflow used to earn it. Import/seed scripts live in `tools/` and `seedFirestore.js` (Admin SDK, bypasses rules — see maintenance log).
- **Pete messages:** Array of `{ id, role: 'user' | 'assistant', text, pantries? }` in component state. Replies come from Gemini (`utils/gemini.ts`, `gemini-flash-latest` via `EXPO_PUBLIC_GEMINI_API_KEY`, PII-sanitized outbound, 10s timeout, in-memory response cache, 20-message session cap) with local keyword responses as fallback; pantry searches skip Gemini entirely and instead run a live Firestore query (`utils/pantries.ts`'s `fetchPantriesByCounty()`, shared with `map.tsx`), filtered to a county named in the message or the last county `map.tsx` inferred from GPS (`utils/userLocation.ts`'s `getLastKnownCounty()`); if neither is known, Pete asks for a city/county instead of guessing, and a zero-result or Firestore-error case says so explicitly rather than falling back to any hardcoded pantry list.

### Payload (future)

- If backend added: define API request/response shapes here.
- If analytics: define event payloads here.

---

## Behavioral rules

1. **Tone:** Helpful, clear, respectful. Alabama Black Belt focus; no jargon.
2. **Do not:** Guess at business logic; change pantry data without updating architecture; add a second logo asset (use single `logo.png`).
3. **Links:** External links open in browser or dialer (211). Map directions: Apple Maps on iOS, Google Maps on Android.
4. **Tab order:** Always Home → Map → Pete → Profile. Entry after auth: `/(tabs)/map`.

---

## Architectural invariants

1. **Expo Router:** Entry is `app/index.tsx`; tabs live under `app/(tabs)/`; auth under `app/(auth)/`. No custom native modules without prebuild.
2. **Assets:** `assets/logo.png` = app icon, splash, and in-app logo. `assets/pete.png` = Pantry Pete avatar. `assets/background.png` = sign-in / header background.
3. **Theme:** `context/ThemeContext.tsx` provides light/dark; every screen including the tab bar (`app/(tabs)/_layout.tsx`) follows it via `theme.card`/`theme.border`/`theme.subtext`. (Previously the tab bar was fixed-light "for visibility" — reversed 2026-08-13 per explicit request.)
4. **Map:** `react-native-maps`; iOS uses MapKit (default provider, optional `mapType="mutedStandard"`).

---

## Maintenance log

| Date       | Change |
|-----------|--------|
| 2026-09-08 | Discovered the 2026-08-25 migration's "populate regional food bank URL for empty websites" fallback had left 833 of 883 active `agencies` (94%) pointing at one of just 7 umbrella regional food-bank homepages (foodbankonline.org, foodbanknorthal.org, feedingthegulfcoast.org, hafb.org, wiregrassfoodbank.com, westalabamafoodbank.org, foodbankofeastalabama.com) instead of a pantry-specific site — "Visit Website" opened a generic regional homepage with no information about that location, reported as pantry websites "not navigable." Added `tools/clearGenericRegionalWebsites.js` (Admin SDK, dry-run supported) and ran it to reset `website` back to `''` on all 833 exact-match docs; `map.tsx` already hides the "Visit Website" button entirely when `website === ''`, so affected pantries now simply show no website button instead of a misleading generic link — same posture as the 2026-09-01 `verified` field correction. Only ~50 pantries with a genuine pantry-specific site were left untouched. |
| 2026-09-08 | Map: added a client-side global search bar (`app/(tabs)/map.tsx`) that filters the already-loaded `pantries` array by name/city/county — deliberately not a Google Places/Geocoding call, since the Google Maps API key was removed from the iOS binary on 2026-08-23 and Geocoding isn't enabled on the Cloud billing account (see 2026-08-17 entry). Replaced the old binary red/grey verified pin scheme with a 3-tier color system (`pantryTier()`): grey (default), orange "Active" (has `phone`/`website`/`socialMedia`), green "Verified" (`verified && operatorPortalAccess && miniProfile`) — added `agencies.socialMedia`, `operatorPortalAccess`, `miniProfile` as optional fields (validated in `firestore.rules` via `validAgencyOptionalFields()`; **rules need `firebase deploy --only firestore:rules`**). `operatorPortalAccess`/`miniProfile` are placeholders for the not-yet-built Agency Operator Portal — nothing sets them yet, so no pantry currently shows green; the existing human-verified `verified` flag is unchanged and still independently shown. Fixed a real Guideline 2.1(a) root cause: `utils/auth.ts`'s app-launch anonymous sign-in (`initAppSecurity()`) could still be in flight when a user submitted real sign-up/sign-in credentials on `app/(auth)/signin.tsx` (children render before `authReady` by design), so `linkOrCreate()` would create a real account via `createUserWithEmailAndPassword()` instead of linking, and the still-pending anonymous `signInAnonymously()` call would then resolve afterward and flip `auth.currentUser` back to a fresh anonymous session — silently orphaning the just-created account. Added `waitForInitialAuthBootstrap()` (awaits the existing in-flight bootstrap promise before `linkOrCreate()`/`signInWithIdentifier()` touch `auth.currentUser`) plus a UI-level gate on `signin.tsx`'s submit button (`authReady`). Added `tools/createDemoReviewAccount.js` — creates/resets a stable `applereview` username + random 6-digit PIN account via Admin SDK for App Store Connect's App Review Information; nothing to bypass since the app has no email verification or 2FA to begin with. |
| 2026-09-01 | Discovered `agencies.verified` was `true` on all 884 docs, but that was an artifact of the 2026-08-25 `resources`→`agencies` migration blanket-setting `verified: true` on every doc during the rename — it had silently overwritten the actual 2026-08-23 Montgomery web-verification result (59 confirmed active / 9 left `uncertain` on purpose) three days later, and no doc anywhere else had ever been individually checked. Reset `verified: false` on all 884 docs, then re-applied only real evidence: the 59 confirmed-active Montgomery pantries (from `tools/.tmp/montgomery_verification_report.md`, replayed via a synthesized `tools/.tmp/verify/result-montgomery.json`) plus 2 pantries from `tools/.tmp/verify/result-03.json` (Baldwin County) — live total now 61 `verified: true` / 823 `false`. `verified` gates cosmetic UI only (map pin color/badge, "call ahead" banner) never visibility, so no pantries were hidden by the reset. Added `tools/applyVerificationResults.js`, the previously-missing script that actually writes `tools/.tmp/verify/result-*.json` web-verification results back to Firestore (`active`→`verified:true` + fills empty `website`; `uncertain`→`verified:false`; `closed`→`verified:false` + `status:'inactive'`, never auto-deletes — flags for human review instead; per-doc `dataIssue` notes append to `tools/.tmp/verify/applied-data-issues.md` rather than polluting the Firestore schema). A statewide 61-batch re-verification pass was already staged in `tools/.tmp/verify/` before this session but only batch 03 (2 pantries) had actually been run; the other 60 batches (~882 pantries) are prepared but not yet executed — run them and `node tools/applyVerificationResults.js` to pick up more real verifications as they complete. |
| 2026-08-27 | Pete pantry-search answers are now grounded in a live Firestore query instead of a hardcoded 9-pantry list that could drift stale — added `utils/pantries.ts` (`fetchPantriesByCounty()`, `extractCounty()`, shared with `map.tsx`'s existing `agencies` query pattern), reuses the existing `county+status` composite index. `app/(tabs)/pete.tsx`'s pantry_search branch now: resolves a target county from the message text or `getLastKnownCounty()`; asks the user for a city/county if neither is known; shows an explicit "no active pantries found" message on zero results; and shows an explicit "couldn't reach the database" message on a Firestore error — never a fabricated list. Also stripped hardcoded pantry names/phones/hours out of the local keyword-fallback `RESPONSES.find_pantries`/`RESPONSES.hours` strings (used only when the Gemini call itself fails) since they could go stale silently. Tightened `utils/gemini.ts`'s `PETE_SYSTEM_PROMPT` to explicitly forbid inventing pantry names/addresses/phone/hours and to forbid asking for or repeating sensitive personal data (SSN, financial, health) — Gemini has no live DB access mid-conversation, so specific pantry facts must come from this dynamic-retrieval path, not the model. |
| 2026-08-25 | Renamed Firestore collection from `resources` to `agencies` across client app screens (`home.tsx`, `map.tsx`, `profile.tsx`), `firestore.rules`, `firestore.indexes.json`, and seed/tool scripts. Ran `tools/migrateResourcesToAgencies.js` to migrate all 884 food assistance locations into `agencies` in Firestore, set `verified: true` across pantries in Alabama counties, and populated regional food bank URLs for empty websites so the map renders red pinpoints (`#b52525`). |
| 2026-08-23 | Added `website` to 37 of 68 Montgomery County `resources` docs, sourced from the same web-verification pass (own-domain / official program pages only — third-party directories and bare Facebook pages excluded). Surfaces via the existing "Visit Website" button in the map detail modal (`app/(tabs)/map.tsx`), which was already wired to `website` but had it empty for nearly all Montgomery entries. |
| 2026-08-23 | Web-search verification pass over all 69 Montgomery County `resources`: for each, searched name/address/phone for a live site, active social media, or third-party pantry-directory listing (not a phone call — see `tools/.tmp/montgomery_verification_report.md` for full per-pantry evidence). 59 had corroborating web presence and were marked `verified: true`. 9 had thin/no web footprint and were left unverified (no evidence of closure, just unconfirmed). "Dalraida United Methodist Church" renamed to "Dalraida Global Methodist Church" — same building/address/pantry, denomination changed in the 2023 UMC/Global Methodist split. "Salvation Army (Montgomery River Region)"'s address/coordinates updated to its 2025 relocation at 3460 Carter Hill Rd (re-geocoded via Census). Deleted "East Lake Community Development" (135 S. Panama St) — its phone number and near-identical siblings all trace to a real but unrelated Birmingham, AL housing nonprofit at a different address; no evidence this org ever existed in Montgomery, so treated as bad import data rather than a closed pantry. |
| 2026-08-23 | Pre-launch audit pass 2 (App Store compliance): added in-app account deletion — required by Guideline 5.1.1(v) since the app offers account creation — via `utils/auth.ts` `deleteAccount()` (deletes `user_profiles/{uid}`, then the Auth record, then re-establishes an anonymous session; handles `auth/requires-recent-login`) surfaced as a Delete Account row on `app/account.tsx` when signed in. `firestore.rules` `user_profiles` now allows owner delete (was `if false`) — deploy rules before release. Added missing `.catch()` to the Profile Legal `Linking.openURL` calls; corrected this file's stale Pete schema line (Gemini-backed since before this session, doc said "mock replies"). |
| 2026-08-23 | Pre-launch audit pass: wired the Profile newsletter toggle (was a no-op `useState`) to AsyncStorage + `user_profiles/{uid}.newsletter` (rules validator updated — deploy `firestore.rules` when convenient; live rules already accept the field); map detail modal now hides the call button / eligibility / docs rows when a pantry has no data (152 FBNA pantries have no phone) and shows fallback text for missing hours; added `.catch()` + user-facing alerts to `tel:`/URL `Linking.openURL` calls (they reject on iPads, which have no dialer); fixed stale "40+ pantries" copy in Pete to 884/67; removed unused `expo-image-picker` dep (was injecting boilerplate camera/mic/photos purpose strings into Info.plist — App Review 5.1.1 risk); removed the Google Maps API key from the iOS binary (iOS uses Apple Maps; Android keeps it); declared collected data types (coarse location, sensitive info, email, device ID, product interaction, other) in the iOS privacy manifest via `app.config.js` `ios.privacyManifests`. |
| 2026-08-17 | Imported 152 pantries across 11 Food Bank of North Alabama (FBNA) counties — Marshall, Franklin, Lauderdale, Colbert, Lawrence, Morgan, Limestone, Madison, Jackson, DeKalb, Cullman — into `resources/` via `tools/addFBNAPantries.js`. Source had no coordinates (geocoded via Census, 18 rural fallbacks to county-seat) and no phone numbers at all (only a staff contact name, left blank). Unlike prior imports, the source's Hours column was detailed enough to extract real per-entry schedules instead of the generic "Call for hours" placeholder. Fixed one row's own County column (Lawrence's "The Helping Hand" read "Madison"), moved one row from the Limestone file to Madison per its own County field ("Good Shepherd UMC"), and collapsed one exact duplicate (Morgan's "Hartselle-Decatur SDA Hispanic Church" listed twice back-to-back). No existing Firestore resources were found in any of these 11 counties beforehand. |
| 2026-08-17 | Imported 227 pantries across 12 Community Food Bank of Central Alabama (CFBCA) counties — Shelby, Etowah, Cherokee, Talladega, Clay, Cleburne, Calhoun, St. Clair, Blount, Jefferson, Walker, Winston — into `resources/` via `tools/addCFBCAPantries.js`, using source coordinates as-given. Applied two exclusion rules not used in prior batches: (1) dropped ~7 "CSFP" (senior commodity program) rows that shared the exact same address as a same-site "Pantry"-named row, keeping the Pantry entry; (2) dropped ~18 rows whose only AgencyGroup category was bare "Residential" (shelters/rehab/senior housing with no pantry or soup-kitchen component) as not being food-distribution points — combo categories like "Residential & Pantry" were kept. No existing Firestore resources were found in any of these 12 counties beforehand. |
| 2026-08-17 | Imported 52 pantries across 8 West Alabama Food Bank (WAFB) counties — Hale, Bibb, Tuscaloosa, Pickens, Fayette, Lamar, Marion, Sumter — into `resources/` via `tools/addWestAlabamaPantries.js`, using the source's own coordinates (no corruption/mismatch pattern this time). "Greene County.pdf" turned out to be a duplicate export of Sumter County's 3 rows under the wrong filename (every row explicitly tagged County=Sumter) — contributed zero real Greene entries; the Sumter trio was written once. No overlaps found against existing Firestore resources in any of the 9 counties checked. |
| 2026-08-17 | Imported 46 pantries across 6 East Alabama counties — Macon, Chambers, Randolph, Tallapoosa, Lee, Russell — into `resources/` via `tools/addEastAlabamaPantries.js`. Source PDFs turned out to be arbitrary slices of one shared unfiltered multi-county query, not per-county exports — every row was filed under its own "County" column value rather than its PDF's filename. The FBEA sub-table's own lat/lng was corrupted for Randolph/Tallapoosa/Russell (real addresses tens of miles apart sharing identical coordinates), so all FBEA rows were re-geocoded from their street address via the Census geocoder instead; the HAFB sub-table's coordinates were trusted as-is (consistent with every prior import). Excluded 1 internal admin record ("HAFB ADMIN - Tuskegee VA") and 6 rows matching pantries already live in Firestore (Barbour's "Forgiven Ministries"; Russell's "St. Patrick Lazarus (Food) Pantry", "Lakewood Baptist Church", "St./John 23rd Center", "Potter's House Baptist (Church Sincere Ministry)"; Macon's "Macon County Food Pantry"). |
| 2026-08-17 | Imported 65 pantries across 6 Wiregrass-region counties — Coffee, Geneva, Dale, Houston, Henry, Barbour — into `resources/` via `tools/addWiregrassPantries.js`. Source had coordinates for only 3 of 65 rows (Barbour's FBEA table); the rest geocoded via the Census geocoder, 7 falling back to county-seat coordinates. Moved "Helping Hands Dothan" from Dale's PDF to Houston County (its own County column and address both say Dothan/Houston, not Dale). Dropped 3 Barbour rows already live in Firestore under matching phone/name: "White Oak UMC", "Eufaula Church of God In Christ", "Bakerhill Community Outreach". |
| 2026-08-17 | Imported 37 pantries across 9 HAFB counties — Lowndes, Elmore, Autauga, Chilton, Coosa, Bullock, Pike, Crenshaw, Butler — into `resources/` via `tools/addBlackBeltHAFBPantries.js`. Corrected 3 source rows where the PDF's own "County" column held a city name instead of the real county (Elmore's "Holy Assembly of Jesus" read "Wetumpka"; Chilton's "Trinity Episcopal Church" read "Clanton"; Crenshaw's "Faith Walk Ministries" read "Lowndes") — each corrected against the row's own city and the file's census-county summary row. No overlaps found against existing Firestore resources in any of the 9 counties. |
| 2026-08-17 | Imported 45 pantries across 4 Selma Area Food Bank counties — Dallas, Perry, Marengo, Wilcox — into `resources/` via `tools/addSelmaAreaPantries.js`. This source had no coordinates, so addresses were geocoded via the free US Census Bureau geocoder after discovering the Google Maps Geocoding API isn't enabled on this project's Cloud billing account (`tools/addMontgomeryPantries.js`'s Google geocoding path is currently broken as a result — first 45-row run silently fell back to 4 identical county-center points and had to be deleted and redone once the Census geocoder was wired in as the primary path). 8 rural addresses the Census geocoder couldn't match still fall back to county-seat coordinates. Dropped 2 rows with no address in the source at all, 3 unrelated animal-shelter/trade-association rows embedded in the Dallas PDF, and 2 rows matching pantries already live in Firestore (one exact, one same-block address match). |
| 2026-08-17 | Imported 157 pantries across 9 Feeding the Gulf Coast (FTGC) counties — Baldwin, Clarke, Choctaw, Conecuh, Covington, Escambia, Mobile, Monroe, Washington — into `resources/` via `tools/addFTGCRegionPantries.js` (Admin SDK, real lat/lng from source). Deduped 2 rows against the 2 FTGC/CFBCA-table entries for the same Theodore, AL food bank site already live in Firestore, plus corrected several source typos (bad area code, invalid ZIP, a mistyped phone confirmed via its sibling location, two mislabeled county values). |
| 2026-08-17 | Imported 34 Montgomery-area pantries from the HAFB Montgomery County dataset into `resources/` via `tools/addHAFBMontgomeryPantries.js` (Admin SDK, real lat/lng from source — no geocoding needed). Deduped against the 36 Montgomery resources already live (12 rows dropped on matching phone or unmistakable name) plus 4 exact-duplicate rows and 1 PO-Box-only row within the source PDF itself. Corrected the stale "Pantries: static array" schema line above — pantries have been Firestore-backed (`resources` collection) since before this session; the doc just hadn't been updated to match. |
| 2026-08-13 | Added optional real email/password accounts on top of the anonymous-auth base (`/(auth)/signin`, previously dead/unreachable code, now reachable from Profile → Account); anonymous→real upgrade preserves uid via `linkWithCredential()`. Added race field to `user_profiles`. |
| 2026-08-13 | Corrected Auth/Profile schema to match actual anonymous-auth architecture (was stale, predated `utils/auth.ts`); added `user_profiles/{uid}` demographic schema; dark-mode theming pass across all screens; added push notifications (`utils/notifications.ts`), which required `npx expo prebuild --clean` per invariant #1 ("no custom native modules without prebuild") — regenerated `ios/` was already untracked/gitignored, so this was low-risk. |
| (today)   | B.L.A.S.T. applied: task_plan, findings, progress, claude.md, architecture/, .tmp/ |
| 2026-08-10 | Entry point reverted to `/(tabs)/map` (was briefly changed to `/(tabs)/home`, then reverted per explicit request) |

---
*Only update this file when a schema, rule, or invariant changes.*
